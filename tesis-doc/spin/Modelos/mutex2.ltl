#define accept2	(mutex != 2)
#define accept3	((x==1 && y==0) || (x==0 && y==1))

	/*
	 * Formula As Typed: [] accept2
	 */

never {    /* ([] accept2) */
accept_init:
T0_init:
	if
	:: ((accept2)) -> goto T0_init
	fi;
}

#ifdef NOTES

#endif
#ifdef RESULT

#endif
